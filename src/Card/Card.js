import React from 'react';

const card = (props) => {
    // console.log(props.coo);
    const { name } = props.cookieData
    console.log(name);
    return (
        <div>
            <h1>heeo</h1>
        </div>
    );
};

export default card;